export declare function parseCollColumn(input: string, source_collection?: string): {
    collection: string;
    column: string;
};
export declare function normalizeCollectionColumn(input: string, source_collection?: string): string;
